apiVersion: v1
kind: Service
metadata:
  name: developeriq-ingestor-service
  namespace: developerid
  labels:
    app: developeriq
spec:
  selector:
    app: developeriq

  ports:
    - port: 8080
      targetPort: 8080

  type: LoadBalancer